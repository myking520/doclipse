<?xml version="1.0" encoding="UTF-8"?>
<doclipse>
<tag target="class" name="@oc4j.bean" doc="All attributes of session-deployment, entity-deployment and message-driven-deployment are placed under this class level tag. ">
<attribute name="jndi-name" doc="This parameter specifies the JNDI name to be used for lookup. It uses the 'ejb-name' by default."/>
<attribute name="pool-cache-timeout" doc="This parameter specifies how long (in seconds) to keep beans cached in the pool. For stateless session beans, if you specify a pool-cache-timeout, then at every pool-cache-timeout interval, all beans in the pool, of the corresponding bean type, are removed. If the value specified is zero or negative, then the pool-cache-timeout is disabled and beans are not removed from the pool."/>
<attribute name="call-timeout" doc="This parameter specifies the maximum time (in milli seconds) to wait for any resource to make a business/life-cycle method invocation. This is not a timeout for how long a business method invocation can take. If the timeout is reached, a TimedOutException is thrown. This excludes database connections. Set to 0 if you want the timeout to be forever."/>
<attribute name="copy-by-value" doc="Whether or not to copy (clone) all the incoming/outgoing parameters in EJB calls. Set this to 'false' if you're certain your application doesn't assume copy-by-value semantics for a speed-up. The default is 'true'."/>
<attribute name="max-instances" doc="The number of maximum bean implementation instances to be kept instantiated or pooled. The default is 100. This setting is valid for session beans (stateless) and entity beans only."/>
<attribute name="min-instances" doc="The number of minimum bean implementation instances to be kept instantiated or pooled. The default is 0. This setting is valid for session (stateless) and entity beans only."/>
<attribute name="max-instances-per-pk" doc="Sets the maximum entity bean wrapper instances allowed in its pool for a given primary key."/>
<attribute name="min-instances-per-pk" doc="Sets the minimum entity bean wrapper instances allowed in its pool for a given primary key."/>
<attribute name="max-tx-retries" doc="The number of times to retry a transaction that was rolled back due to system-level failures. The default is 3."/>
<attribute name="persistence-filename" doc="Path to file where sessions are stored across restarts."/>
<attribute name="timeout" doc="Inactivity timeout in seconds. If the value is zero or negative timeouts will be disabled."/>
<attribute name="clustering-schema" required="true" allowed="default,asynchronous-cache" doc="This parameter should be set to &quot;asynchronous-cache&quot; in order to cluster-enable an Entity bean."/>
<attribute name="data-source" required="true" doc="The name of the data-source used if using container-managed persistence. Use the 'ejb-location' attribute of the data-source entry."/>
<attribute name="exclusive-write-access" doc="Whether or not the EJB-server has exclusive write (update) access to the database backend. This can be used only for entity beans that use a &quot;read_only&quot; locking mode. In this case, it increases the performance for common bean operations and enables better caching. Default is false for beans with locking-mode=optimistic or pessimistic and true for locking-mode=read-only. "/>
<attribute name="instance-cache-timeout" doc="The amount of time in seconds that entity wrapper instances are assigned to an identity. If you specify 'never', you retain the wrapper instances until they are garbage collected. The default is 60 seconds."/>
<attribute name="isolation" allowed="none,committed,serializable,uncommitted,repeatable_reads" doc="Specifies the isolation-level for database actions. The valid values for Oracle databases are 'serializable' and 'committed'. The default is 'committed'. Non-Oracle databases can be the following: 'none', 'committed', 'serializable', 'uncommitted', and 'repeatable_reads'."/>
<attribute name="table" doc="The name of the table in the database if using container-managed persistence. Applicable to entity beans."/>
<attribute name="validity-timeout" doc="The maximum amount of time (in millis) that an entity is valid in the cache (before being reloaded). Useful for loosely coupled environments where rare updates from legacy systems occur. This attribute is only valid when exclusive-write-access=&quot;true&quot; (the default). Applicable to entity beans."/>
<attribute name="instance-cache-timeout" doc="The amount of time in seconds that entity wrapper instances are assigned to an identity. If you specify 'never', you retain the wrapper instances until they are garbage collected. The default is 60 seconds."/>
<attribute name="do-select-before-insert" doc="Recommend setting to false to avoid the extra select before insert which checks if the entity already exists before doing the insert. This will then detect a duplicate, if there is one, during the insert. Default Value: true"/>
<attribute name="locking-mode" allowed="pessimistic,optimistic,read-only,old_pessimistic" doc="Configures the handling of resource contention and parallel execution within CMP entity beans. PESSIMISTIC uses &quot;select for update&quot; to manage resource contention and only allows one user at a time to execute the entity bean. OPTIMISTIC allows multiple users to execute the entity bean in parallel. It does not monitor resource contention; thus, the burden of the data consistency is placed on the database isolation modes. READ-ONLY allows multiple users to execute the entity bean in parallel. The container does not allow any updates to the bean's state. OLD_PESSIMISTIC is maintained for backward compatibility with OC4J 1.0.2.2 and for use with non-Oracle databases."/>
<attribute name="update-changed-fields-only" doc="Specifies whether the container updates only modified fields or all fields to persistence storage for CMP entity beans when ejbStore is invoked. The default is true, which specifies to only update modified fields."/>
<attribute name="disable-wrapper-cache" doc="If true, a pool of wrapper instances is not maintained. The default is true."/>
<attribute name="delay-updates-until-commit" doc="This attribute is valid only for CMP entity beans. Defers the flushing of transactional data until commit time or not. The default is true. Set this value to false to update persistence data after completion of every EJB method invocation - except ejbRemove() and the finder methods."/>
<attribute name="connection-factory-location" required="true" doc="The JMS Destination Connection Factory is specified in the connection-factory-location attribute. The syntax for database queue/topic is &quot;java:comp/resource&quot; + resource provider name + &quot;TopicConnectionFactories&quot; or &quot;QueueConnectionFactories&quot; + user defined name. The xxxConnectionFactories details what type of factory is being defined."/>
<attribute name="destination-location" required="true" doc="The JNDI location of the destination (queue/topic) to use. The JMS Destination is specified in the destination-location attribute. The syntax for database queue/topic is &quot;java:comp/resource&quot; + resource provider name + &quot;Topics&quot; or &quot;Queues&quot; + Destination name. The Topic or Queue details what type of Destination is being defined. The Destination name is the actual queue or topic name defined in the database."/>
<attribute name="subscription-name" doc="If this is a topic, the subscription name is defined in the subscription-name attribute."/>
<attribute name="listener-threads" doc="The listener threads are used to concurrently consume JMS messages. The default is one thread."/>
<attribute name="transaction-timeout" doc="This attribute controls the transaction timeout interval for any container-managed transactional MDB. The default is one day. If the transaction has not completed in this timeframe, the transaction is rolled back."/>
</tag>
<tag target="method" name="@oc4j.persistence" doc="Use these tags to specify persistence details for Container Managed Entity Beans.">
<attribute name="persistence-name" doc="The name of the field in the database table."/>
<attribute name="persistence-type" doc="The database type (valid values varies from database to database) of the field."/>
<attribute name="table-name" doc="The database table name for this field"/>
<attribute name="immutable" doc="Whether or not the value can be trusted to be immutable once added to the Collection/Map. Setting this to true will optimize database operations extensively. The default value is &quot;true&quot; for set-mapping and map-mappings and &quot;false&quot; for collection-mapping and list-mapping."/>
</tag>
<tag target="method" name="@oc4j.field-persistence-manager" doc="If @oc4j.persistence field-persistence-manager specified and there's some properties for the persistence manager, specify it as one or more @oc4j.field-persistence-manager-property tags for the persistent field. ">
<attribute name="class" doc="The class that implements a custom persistence handler for this CMP-field."/>
</tag>
<tag target="method" name="@oc4j.field-persistence-manager-property" doc="Specify properties for field-persistence-manager. Must have a @oc4j.field-persistence-manager &quot;class&quot; param set.">
<attribute name="name" doc="The name of the property."/>
<attribute name="value" doc="The value of the property."/>
</tag>
</doclipse>
